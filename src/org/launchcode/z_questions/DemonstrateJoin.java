package org.launchcode.z_questions;

/**
 * join method can be used to pause the current thread execution until unless the specified thread is dead.
 * <p>
 * There are three overloaded join functions.join method can be used to pause the current thread execution
 * until unless the specified thread is dead. There are three overloaded join functions.
 * <p>
 * Write a program to Demonstrate Join on Main Thread
 */
public class DemonstrateJoin {
}
